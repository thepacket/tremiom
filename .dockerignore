node_modules
dist
*.tsbuildinfo

# Python virtualenv from local dev; the image builds its own under /opt/venv.
workers/.venv
**/__pycache__
*.pyc

# NB: .git is intentionally NOT ignored — the frontend build reads the
# commit count + short SHA from it to bake into the version label
# (vite.config.ts / src/version.ts).
.github
.claude
.DS_Store
*.log
.vite
.env
.env.local
